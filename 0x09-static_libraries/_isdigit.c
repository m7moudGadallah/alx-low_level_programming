#include "main.h"

/**
  * _isdigit - check if passed char is digit or not
  *
  * @c: char we need to check
  *
  * Return: (1) char is digit, (0) otherwise
  */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
