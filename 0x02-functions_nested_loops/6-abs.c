#include "main.h"

/**
  * _abs - to compute abslute value of an integer
  *
  * @n : number we need to get it's absloute value
  *
  * Return: absolute value of n
  */
int _abs(int n)
{
	return ((n < 0) ? -n : n);
}
