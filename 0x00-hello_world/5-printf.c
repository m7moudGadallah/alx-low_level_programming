#include <stdio.h>

/**
  * main - A function is used to print sentence with using `printf` function
  * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
